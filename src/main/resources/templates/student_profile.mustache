<!DOCTYPE html>
<!doctype html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>Student Info</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

<div class="container d-flex justify-content-center align-items-center" style="min-height: 100vh;">
    <div class="card shadow p-4" style="width: 400px;">
        <h4 class="card-title text-center mb-4">{{student.name}} 학생의 정보</h4>
        <div class="card-body">
            <p><strong>이름:</strong> {{student.name}}</p>
            <p><strong>학번:</strong> {{student.studentNumber}}</p>
            <p><strong>이메일:</strong> {{student.email}}</p>
            <p><strong>전공:</strong> {{student.major}}</p>
            <p id="grade"><strong>학년:</strong> {{student.grade}}</p>
        </div>
        <div class="d-flex justify-content-between mt-3">
            <a href="/student/profile/edit" class="btn btn-primary btn-sm">정보 수정</a>
            <a href="/student/dashboard" class="btn btn-secondary btn-sm">대시보드로</a>
        </div>
    </div>
</div>

</body>
</html>


<script>

    const grade = "{{student.grade}}";

  const gradeText = /^[1-4]$/.test(grade) ? grade + "학년" : grade;
  document.getElementById("grade").innerText = "학년 : " + gradeText;

</script>