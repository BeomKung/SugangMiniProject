<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" />
    <title>mini_project</title>
</head>
<body>

<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="http://localhost:8080/admin">관리자 페이지</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <div class="ms-auto">
                <a href="/admin" class="btn btn-danger">관리자 페이지 돌아가기</a>
            </div>
        </div>
    </div>
</nav>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

<div class="container mt-5">
    <h2 class="text-center mb-5">시스템 설정</h2>

    <label for="title" style="font-size: 24px; display: block; margin-bottom: 10px;">
        수강신청 기간 설정
    </label>

    <div class="d-flex align-items-center mb-3 gap-3">
        <div>
            <label for="startDate" class="form-label mb-0">시작 날짜:</label><br />
            <input type="date" id="startDate" name="startDate" class="form-control" />
        </div>

        <div>
            <label for="endDate" class="form-label mb-0">종료 날짜:</label><br />
            <input type="date" id="endDate" name="endDate" class="form-control" />
        </div>

        <div class="align-self-end">
            <button type="button" class="btn btn-primary">적용하기</button>
        </div>
    </div>

    <hr class="my-5" />

    <label for="title" style="font-size: 24px; display: block; margin-top: 50px; margin-bottom: 10px;">
        수강신청 제한 조건 설정
    </label>

    <table class="table table-bordered">
        <thead>
        <tr>
            <th>과목명</th>
            <th>학과</th>
            <th>학년</th>
        </tr>
        </thead>
        <tbody>
        <tr>
            <td>{{subject.subjectName}}</td>
            <td>
                <label><input type="checkbox" name="major_{{subject.subjectName}}" value="영어영문학과"> 영문과</label><br>
                <label><input type="checkbox" name="major_{{subject.subjectName}}" value="컴퓨터공학과"> 컴퓨터공학과</label><br>
                <label><input type="checkbox" name="major_{{subject.subjectName}}" value="디자인학과"> 디자인학과</label><br>
                <label><input type="checkbox" name="major_{{subject.subjectName}}" value="교양학부"> 교양학부</label>
            </td>
            <td>
                <label><input type="checkbox" name="grade_{{subject.subjectName}}" value="1학년"> 1학년</label><br>
                <label><input type="checkbox" name="grade_{{subject.subjectName}}" value="2학년"> 2학년</label><br>
                <label><input type="checkbox" name="grade_{{subject.subjectName}}" value="3학년"> 3학년</label><br>
                <label><input type="checkbox" name="grade_{{subject.subjectName}}" value="4학년"> 4학년</label><br>
                <label><input type="checkbox" name="grade_{{subject.subjectName}}" value="석사과정"> 석사과정</label><br>
                <label><input type="checkbox" name="grade_{{subject.subjectName}}" value="박사과정"> 박사과정</label>
            </td>
        </tr>
    </table>

    <div style="text-align: right; margin-top: 10px;">
        <button type="button" class="btn btn-primary">적용하기</button>
    </div>



{{> layouts/footer}}